/**
 * \page beamgen beam\_gen
 * 
 * Example script to generate beam events. The generated events are transformed to beam coordinates and randomly sampled into a final output stdhep file.
 * 
 * \section params Job parameters
 * There are no special run parameters that need to be set for this job other than the general ones discussed on the \ref examples "examples main page".
 */

