/**
 * \page apgentoslic ap\_gen\_to\_slic
 * 
 * By modifying the job script (including subsequent recompilation of hps-mc), this example can also be used to produce prompt or delayed signals.
 * For the delayed signals, the parameter “ctau” needs to be set in jobs.json.
 * 
 * \section params Job parameters
 * This table contains parameters special to the ap\_gen\_to\_slic job. The general parameters are discussed on the \ref examples "examples main page".
 * 
 * | param    |                              |
 * |----------|------------------------------|
 * | apmass   | A-prime mass in MeV?         |
 * | detector | detector setup used for slic |
 */
